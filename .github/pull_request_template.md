## What this changes



## Checklist

- [ ] `python tests/test_basics.py` passes
- [ ] New user-facing strings go through `tr()` and are added to `locales/en.json`
- [ ] For a translation: `_meta.code` matches the file name, all keys present, `{placeholders}` unchanged
- [ ] For a skin: readable in normal and compact mode, with and without album art
